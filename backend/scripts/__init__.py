"""Standalone CLIs: `seed.py` (demo data) and their shared `factories.py`
helpers. Not imported by `app` — these are dev/deploy-time tools (spec
§12.4), run via `uv run python -m scripts.seed` / `make seed`.
"""
